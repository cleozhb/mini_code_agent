"""缓存工具模块（占位）.

TODO: 在这里实现 `cached` 装饰器和 `clear_cache` 函数。

契约（也在 task 描述里）：
- `cached(func)` 返回一个和 func 同签名的 wrapper，按 (func, args, kwargs) 做 key 缓存
- `clear_cache()` 清掉所有 cached 函数累积的条目
"""

from __future__ import annotations
